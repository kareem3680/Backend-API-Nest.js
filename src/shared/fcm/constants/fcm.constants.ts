export const FCM_ERROR_MESSAGES = {
  TOKEN_REQUIRED: 'FCM token is required',
  SEND_FAILED: 'FCM send failed',
  BATCH_SEND_FAILED: 'FCM batch send failed',
} as const;

export const FCM_BATCH_SIZE = 500;
